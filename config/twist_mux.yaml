twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : cmd_vel_nav      # /cmd_vel değil! (feedback loop önlenir)
        timeout : 0.5
        priority: 10

      obstacle:
        topic   : cmd_vel_obstacle
        timeout : 0.5
        priority: 15

      tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 20

      joystick:
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 100
